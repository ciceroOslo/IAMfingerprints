# This file was generated by Jan Ivar Korsbakken <janko@cicero.oslo.no> on
# 2023-12-27 to keep track of Python dependencies. It is not part of the
# original code base published by Mark Dekker.

[tool.poetry]
name = "iamfingerprints"
version = "1.1"
description = "Suite for calculating model diagnostics and computing model fingerprint diagrams"
authors = ["Mark Dekker"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.11,<3.12"
PyYAML = "^6.0.1"
pandas = "^2.1.4"
xarray = "^2023.12.0"
numpy = "*"  # Don't pin version, other packages have complex version pins
matplotlib = "*"  # Don't pin version, other packages have complex version pins
tqdm = "^4.66.1"
pyam-iamc = "^2.1.0"

[tool.poetry.group.dev.dependencies]
ipython = "^8.19.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
